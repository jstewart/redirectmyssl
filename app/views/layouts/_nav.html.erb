<nav class="bg-gray-100 flex justify-between items-center p-5 mb-5">
  <a class="font-bold text-2xl lg:text-4xl" href="/"> RedirectMySSL </a>
  <div class="block lg:hidden">
    <button
      class="flex items-center px-3 py-2 border rounded text-gray-500 border-gray-600 hover:text-gray-800 hover:border-teal-500 appearance-none focus:outline-none"
    >
      <svg
        class="fill-current h-3 w-3"
        viewBox="0 0 20 20"
        xmlns="http://www.w3.org/2000/svg"
      >
        <title>Menu</title>
        <path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z" />
      </svg>
    </button>
    <!-- TODO: Make the mobile menu -->
  </div>
  <div class="hidden lg:block">
    <ul class="inline-flex">
      <% if user_signed_in? %>
        <li>
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "px-4 hover:text-gray-800" %>
        </li>
      <% else %>
        <li>
          <%= link_to "Log in", new_user_session_path, class: "px-4 hover:text-gray-800" %>
        </li>
      <% end %>
      <li><a class="px-4 hover:text-gray-800" href="#">About</a></li>
      <li><a class="px-4 hover:text-gray-800" href="#">Contact</a></li>
    </ul>
  </div>
</nav>
